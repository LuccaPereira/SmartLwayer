# ===============================================
# PERFIL DE TESTES
# ===============================================

spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: create-drop

  flyway:
    enabled: false # Desabilitado em testes (usa create-drop)

logging:
  level:
    root: WARN
    smartLegalApi: DEBUG

app:
  jwt:
    secret: test-secret-key
    expiration: 3600000 # 1 hora em testes

